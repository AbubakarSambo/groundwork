[build]
builder = "NIXPACKS"
buildCommand = "npm run build && npx prisma generate"

[deploy]
startCommand = "npx prisma migrate deploy && npm run prisma:seed && node dist/main"
healthcheckPath = "/health"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
